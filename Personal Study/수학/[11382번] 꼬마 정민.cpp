#include <bits/stdc++.h>
using namespace std;

using ull = unsigned long long;

int main(void) {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    ull A, B, C;
    cin >> A >> B >> C;
    ull sum = A + B + C;
    cout << sum;
    return 0;
}
